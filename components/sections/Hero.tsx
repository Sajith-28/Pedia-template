import { HeroBanner } from "./HeroBanner";

/**
 * Hero section for Dr. Ushapriya Sudhakar.
 * Features the responsive, auto-changing banner carousel with client photography,
 * mobile/desktop optimized assets, clear credentials, and conversion CTAs.
 */
export function Hero() {
  return <HeroBanner />;
}
